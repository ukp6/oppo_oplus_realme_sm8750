name: 加速版构建 6.6.89 欧加真OKI内核 自用

env:
  TZ: Asia/Shanghai
  ANDROID_VERSION: 'android15'
  KERNEL_VERSION: '6.6'
  KERNEL_NAME: 'android15-8-g29d86c5fc9dd-abogki428889875-4k'

on:
  workflow_dispatch:
    inputs:
      ksu_type:
        description: 'KernelSU分支(SukiSU Ultra/ReSukiSU/KernelSU Next/MKSU/KSU(原版)/无内置KSU,默认ReSukiSU)'
        required: true
        type: choice
        default: 'ksunext'
        options:
          - 'sukisu'
          - 'resukisu'
          - 'ksunext'
          - 'mksu'
          - 'ksu'
          - 'none'
      susfs_enable:
        description: '是否开启susfs(用于增强隐藏环境挂载功能; 可能轻微增加耗电，上游更新导致不稳定时或不需要可关闭)'
        required: true
        type: boolean
        default: 'false'
      kpm_enable:
        description: '是否开启kpm(builtin-使用(re)sukisu内置kpm, kpn-使用独立kpm实现(支持任意KSU/面具环境); 不需要可保持默认关闭)'
        required: true
        type: choice
        default: 'false'
        options:
          - 'false'
          - 'builtin'
          - 'kpn'
      lz4_enable:
        description: '是否安装 lz4 '
        required: true
        type: boolean
        default: 'true'
      lz4kd_enable:
        description: '是否安装 LZ4KD 补丁(若已开启lz4补丁则可不开启)'
        required: true
        type: boolean
        default: 'false'
      bbr_enable:
        description: "是否启用bbr算法(优化上行数据,对手机日用无太大意义甚至可能负优化;false关闭,true仅加入算法,default启用算法并设为默认)"
        required: true
        type: choice
        default: 'false'
        options:
          - 'false'
          - 'true'
          - 'default'
      better_net:
        description: '是否开启网络功能拓展配置(用于为ipset及需要iptables等高级网络功能内核支持的程序提供支持)'
        required: true
        type: boolean
        default: 'false'
      adios_enable:
        description: '是否启用ADIOS IO调度器支持(提升IO读写性能)'
        required: true
        type: boolean
        default: 'true'
      rekernel_enable:
        description: '是否启用Re-Kernel支持(与Freezer/NoActive等软件配合, 提升应用冻结能力)'
        required: true
        type: boolean
        default: 'false'
      baseband_guard:
        description: '是否开启内核级基带保护(阻止一切对非用户分区的写入，有效防止格机)'
        required: true
        type: boolean
        default: 'false'
      ccache_debug:
        description: '是否上传 Ccache调试日志(用于调试, 无需要不必开启)'
        required: true
        type: boolean
        default: 'false'
      kernel_suffix:
        description: '内核后缀(留空默认,开头别加连字符,勿加空格等影响指令运行的保留字符)'
        required: false
        type: string
        default: ''

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      ksuver: ${{ steps.ksu_version.outputs.ksuver }}
    steps:
      - name: 安装环境依赖+初始化源码仓库及llvm-Clang18工具链
        run: |
          rm -rf kernel_workspace
          mkdir kernel_workspace
          cd kernel_workspace
          
          sudo apt-mark hold firefox &&
          sudo apt-mark hold libc-bin &&
          sudo apt purge man-db &&
          sudo rm -rf /var/lib/man-db/auto-update &&
          sudo apt update &&
          sudo apt-get install -y --no-install-recommends binutils python-is-python3 libssl-dev libelf-dev ccache &
          
          echo "正在克隆源码仓库..."
          aria2c -s16 -x16 -k1M https://github.com/cctv18/android_kernel_common_oneplus_sm8750/archive/refs/heads/oneplus/sm8750_v_16.0.0_oneplus_13_6.6.89.zip -o common.zip && 
          unzip -q common.zip && 
          mv "android_kernel_common_oneplus_sm8750-oneplus-sm8750_v_16.0.0_oneplus_13_6.6.89" common &&
          rm -rf common.zip &
          
          echo "正在克隆llvm-clang18工具链..." &&
          mkdir -p clang18 &&
          aria2c -s16 -x16 -k1M https://github.com/cctv18/oneplus_sm8650_toolchain/releases/download/LLVM-Clang18-r510928/clang-r510928.zip -o clang.zip &&
          unzip -q clang.zip -d clang18 &&
          rm -rf clang.zip &
          
          echo "正在克隆构建工具..." &&
          aria2c -s16 -x16 -k1M https://github.com/cctv18/oneplus_sm8650_toolchain/releases/download/LLVM-Clang18-r510928/build-tools.zip -o build-tools.zip &&
          unzip -q build-tools.zip &&
          rm -rf build-tools.zip &
          
          wait
          echo "所有源码及llvm-Clang18工具链初始化完成！"
          echo "正在去除 ABI 保护 & 去除 dirty 后缀..."
          rm common/android/abi_gki_protected_exports_* || true
          for f in common/scripts/setlocalversion; do
            sed -i 's/ -dirty//g' "$f"
            sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' "$f"
          done

      - name: 配置ccache目录
        run: |
          echo "CCACHE_DIR=$HOME/.ccache_6.6.89" >> $GITHUB_ENV
          echo "CCACHE_MAXSIZE=3G" >> $GITHUB_ENV
          echo "当前磁盘空间："
          df -h
          echo "当前构建内核版本：6.6.89"

      - name: 载入当前版本内核的 ccache缓存
        uses: actions/cache@v4
        id: ccache-restore
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-neov4-6.6.89-${{ runner.os }}-main
          restore-keys: |
            ccache-neov4-6.6.89-${{ runner.os }}-
            ccache-neov4-6.6.89-
      
      - name: 初始化并配置ccache
        run: |
          export CCACHE_COMPILERCHECK="none"
          export CCACHE_BASEDIR="${{ github.workspace }}"
          export CCACHE_NOHASHDIR="true"
          export CCACHE_HARDLINK="true"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CCACHE_MAXSIZE="${{ env.CCACHE_MAXSIZE }}"
          
          mkdir -p "$CCACHE_DIR"
          
          echo "配置ccache缓存大小为: $CCACHE_MAXSIZE"
          ccache -M "$CCACHE_MAXSIZE"
          ccache -o compression=true
          
          echo "ccache初始状态:"
          ccache -s
          
          if [ "${{ steps.ccache-restore.outputs.cache-hit }}" == 'true' ]; then
            echo "ccache缓存命中详情:"
            ccache -sv
          fi
        
      - name: 添加KernelSU
        id: ksu_version
        run: |
          cd kernel_workspace
          if [[ ${{ github.event.inputs.ksu_type }} == "sukisu" ]]; then
            echo "正在配置SukiSU Ultra..."
            curl -LSs "https://raw.githubusercontent.com/ShirkNeko/SukiSU-Ultra/refs/heads/main/kernel/setup.sh" | bash -s builtin
            cd ./KernelSU
            GIT_COMMIT_HASH=$(git rev-parse --short=8 HEAD)
            echo "当前提交哈希: $GIT_COMMIT_HASH"
            export KSU_VERSION=$KSU_VERSION
            
            for i in {1..3}; do
              KSU_API_VERSION=$(curl -s "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/builtin/kernel/Kbuild" | 
                grep -m1 "KSU_VERSION_API :=" | 
                awk -F'= ' '{print $2}' | 
                tr -d '[:space:]')
              [ -n "$KSU_API_VERSION" ] && break || sleep 1
            done
            [ -z "$KSU_API_VERSION" ] && KSU_API_VERSION="3.1.7"
            
            echo "KSU_API_VERSION=$KSU_API_VERSION" >> $GITHUB_ENV
            
            VERSION_DEFINITIONS=$'define get_ksu_version_full\nv\\$1-'"$GIT_COMMIT_HASH"$'@cctv18\nendef\n\nKSU_VERSION_API := '"$KSU_API_VERSION"$'\nKSU_VERSION_FULL := v'"$KSU_API_VERSION"$'-'"$GIT_COMMIT_HASH"$'@cctv18'
            
            sed -i '/define get_ksu_version_full/,/endef/d' kernel/Kbuild
            sed -i '/KSU_VERSION_API :=/d' kernel/Kbuild
            sed -i '/KSU_VERSION_FULL :=/d' kernel/Kbuild
            
            awk -v def="$VERSION_DEFINITIONS" '
              /REPO_OWNER :=/ {print; print def; inserted=1; next}
              1
              END {if (!inserted) print def}
            ' kernel/Kbuild > kernel/Kbuild.tmp && mv kernel/Kbuild.tmp kernel/Kbuild

            KSU_VERSION=$(expr $(git rev-list --count main) + 37185 2>/dev/null || echo 114514)
            echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
            echo "ksuver=$KSU_VERSION" >> $GITHUB_OUTPUT

            grep -A10 "REPO_OWNER" kernel/Kbuild
            grep "KSU_VERSION_FULL" kernel/Kbuild
            echo "SukiSU版本号: v${KSU_API_VERSION}-${GIT_COMMIT_HASH}@cctv18"
            
          elif [[ ${{ github.event.inputs.ksu_type }} == "resukisu" ]]; then
            echo "正在配置ReSukiSU..."
            curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/refs/heads/main/kernel/setup.sh" | bash -s main
            echo 'CONFIG_KSU_FULL_NAME_FORMAT="%TAG_NAME%-%COMMIT_SHA%@cctv18"' >> ./common/arch/arm64/configs/gki_defconfig
            cd ./KernelSU
            KSU_VERSION=$(expr $(git rev-list --count main) + 30700 2>/dev/null || echo 114514)
            echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
            echo "ksuver=$KSU_VERSION" >> $GITHUB_OUTPUT
            
          elif [[ ${{ github.event.inputs.ksu_type }} == "ksunext" ]]; then
            echo "正在配置KernelSU Next (dev分支)..."
            # 使用 dev 分支
            curl -LSs "https://raw.githubusercontent.com/pershoot/KernelSU-Next/refs/heads/dev/kernel/setup.sh" | bash -s dev
            cd KernelSU-Next
            
            # 更可靠的版本号获取方式
            KSU_VERSION=$(expr $(curl -sI "https://api.github.com/repos/pershoot/KernelSU-Next/commits?sha=dev&per_page=1" | grep -i "link:" | sed -n 's/.*page=\([0-9]*\)>; rel="last".*/\1/p') "+" 30000)
            echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
            echo "ksuver=$KSU_VERSION" >> $GITHUB_OUTPUT
            sed -i "s/KSU_VERSION_FALLBACK := 1/KSU_VERSION_FALLBACK := $KSU_VERSION/g" kernel/Kbuild
            
            KSU_GIT_TAG=$(curl -sL "https://api.github.com/repos/KernelSU-Next/KernelSU-Next/tags" | grep -o '"name": *"[^"]*"' | head -n 1 | sed 's/"name": "//;s/"//')
            sed -i "s/KSU_VERSION_TAG_FALLBACK := v0.0.1/KSU_VERSION_TAG_FALLBACK := $KSU_GIT_TAG/g" kernel/Kbuild
            
          elif [[ ${{ github.event.inputs.ksu_type }} == "mksu" ]]; then
            echo "正在配置 MKSU (5ec1cff/KernelSU)..."
            curl -LSs "https://raw.githubusercontent.com/5ec1cff/KernelSU/refs/heads/main/kernel/setup.sh" | bash -s main
            cd ./KernelSU
            KSU_VERSION=$(expr $(curl -sI "https://api.github.com/repos/5ec1cff/KernelSU/commits?sha=main&per_page=1" | grep -i "link:" | sed -n 's/.*page=\([0-9]*\)>; rel="last".*/\1/p') "+" 30000)
            echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
            echo "ksuver=$KSU_VERSION" >> $GITHUB_OUTPUT
            sed -i "s/DKSU_VERSION=16/DKSU_VERSION=${KSU_VERSION}/" kernel/Kbuild
            
          elif [[ ${{ github.event.inputs.ksu_type }} == "ksu" ]]; then
            echo "正在配置原版 KernelSU (tiann/KernelSU)..."
            curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/refs/heads/main/kernel/setup.sh" | bash -s main
            cd ./KernelSU
            KSU_VERSION=$(expr $(curl -sI "https://api.github.com/repos/tiann/KernelSU/commits?sha=main&per_page=1" | grep -i "link:" | sed -n 's/.*page=\([0-9]*\)>; rel="last".*/\1/p') "+" 30000)
            echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
            echo "ksuver=$KSU_VERSION" >> $GITHUB_OUTPUT
            sed -i "s/DKSU_VERSION=16/DKSU_VERSION=${KSU_VERSION}/" kernel/Kbuild
            
          else
            echo "已选择无内置KernelSU模式，跳过KernelSU配置..."
            echo "KSUVER=0" >> $GITHUB_ENV
            echo "ksuver=0" >> $GITHUB_OUTPUT
          fi
          
      - name: 应用 KernelSU & SUSFS 补丁
        if: inputs.susfs_enable
        run: |
          cd kernel_workspace
          KSU_TYPE="${{ github.event.inputs.ksu_type }}"
          ANDROID_VER="${{ env.ANDROID_VERSION }}"
          KERNEL_VER="${{ env.KERNEL_VERSION }}"
          
          # 克隆 SUSFS 补丁
          git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git -b "gki-${ANDROID_VER}-${KERNEL_VER}"
          wget -q https://github.com/cctv18/oppo_oplus_realme_sm8750/raw/refs/heads/main/other_patch/69_hide_stuff.patch -O ./common/69_hide_stuff.patch
          
          # 复制通用文件
          cp ./susfs4ksu/kernel_patches/50_add_susfs_in_gki-${ANDROID_VER}-${KERNEL_VER}.patch ./common/
          cp ./susfs4ksu/kernel_patches/fs/* ./common/fs/ 2>/dev/null || true
          cp ./susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/ 2>/dev/null || true
          
          cd common
          
          # 应用主 SUSFS 补丁（带重试和模糊匹配）
          echo "应用 SUSFS 主补丁..."
          if patch -p1 -N --fuzz=3 < "50_add_susfs_in_gki-${ANDROID_VER}-${KERNEL_VER}.patch"; then
            echo "✓ SUSFS 主补丁应用成功"
          else
            echo "⚠️ SUSFS 主补丁应用失败，尝试强制应用..."
            patch -p1 -N --fuzz=5 -l < "50_add_susfs_in_gki-${ANDROID_VER}-${KERNEL_VER}.patch" || echo "⚠️ SUSFS 主补丁最终失败"
          fi
          
          # 应用 69_hide_stuff 补丁
          echo "应用 69_hide_stuff 补丁..."
          if [ -f "69_hide_stuff.patch" ]; then
            if patch -p1 -N --fuzz=3 < "69_hide_stuff.patch"; then
              echo "✓ 69_hide_stuff 补丁应用成功"
            else
              echo "⚠️ 69_hide_stuff 补丁应用失败，继续构建"
            fi
          fi
          
          # ReSukiSU 特定修复
          if [ "$KSU_TYPE" = "resukisu" ]; then
            if [ -f fs/proc/task_mmu.c ]; then
              if ! grep -q "struct vm_area_struct \\*vma = find_vma" fs/proc/task_mmu.c; then
                sed -i 's|vma = find_vma(mm, addr)|struct vm_area_struct *vma = find_vma(mm, addr)|g' fs/proc/task_mmu.c
                echo "✓ 已应用 ReSukiSU 编译修复"
              fi
            fi
          fi
          
          # MKSU / 原版 KSU 的特殊补丁
          if [ "$KSU_TYPE" = "mksu" ] || [ "$KSU_TYPE" = "ksu" ]; then
            cd ../KernelSU
            if [ -f ../susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch ]; then
              cp ../susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch .
              # 修复补丁中的 Makefile -> Kbuild 路径变更
              sed -i 's|kernel/Makefile|kernel/Kbuild|g' 10_enable_susfs_for_ksu.patch 2>/dev/null || true
              if patch -p1 -N --fuzz=3 < 10_enable_susfs_for_ksu.patch; then
                echo "✓ SUSFS for KSU 补丁应用成功"
              else
                echo "⚠️ SUSFS for KSU 补丁应用失败"
              fi
            fi
            
            if [ "$KSU_TYPE" = "mksu" ]; then
              wget -q https://github.com/cctv18/oppo_oplus_realme_sm8750/raw/refs/heads/main/other_patch/mksu_supercalls.patch
              patch -p1 -N --fuzz=3 < mksu_supercalls.patch || echo "⚠️ mksu_supercalls 补丁应用失败"
            fi
            cd ../common
          fi
          
      - name: 应用lz4 1.10.0补丁
        if: inputs.lz4_enable
        run: |
          echo "正在添加lz4 1.10.0补丁…"
          cd kernel_workspace
          git clone --depth=1 https://github.com/ukp6/oppo_oplus_realme_sm8750.git
          cp ./oppo_oplus_realme_sm8750/zram_patch/001-lz4.patch ./common/
          cp ./oppo_oplus_realme_sm8750/zram_patch/lz4armv8.S ./common/lib
          cd ./common
          git apply -p1 < 001-lz4.patch || true
          echo "✅ lz4 1.10.0 补丁应用完成（原版方案）"
          
      - name: 应用 lz4kd  补丁
        if: inputs.lz4kd_enable
        run: |
          echo "正在添加lz4kd 补丁…"
          cd kernel_workspace
          if [ ! -d "SukiSU_patch" ]; then
            git clone --depth=1 https://github.com/ShirkNeko/SukiSU_patch.git
          fi
          cd common
          cp -r ../SukiSU_patch/other/zram/lz4k/include/linux/* ./include/linux/ 2>/dev/null || true
          cp -r ../SukiSU_patch/other/zram/lz4k/lib/* ./lib 2>/dev/null || true
          cp -r ../SukiSU_patch/other/zram/lz4k/crypto/* ./crypto 2>/dev/null || true
          PATCH_FILE="../SukiSU_patch/other/zram/zram_patch/6.6/lz4kd.patch"
          if [ -f "$PATCH_FILE" ]; then
            cp "$PATCH_FILE" ./
            if patch -p1 -N -F 3 < lz4kd.patch; then
              echo "✅ lz4kd 补丁应用成功"
            else
              echo "⚠️ lz4kd 补丁应用失败，继续构建"
            fi
          else
            echo "⚠️ lz4kd 补丁文件不存在，跳过"
          fi
          cd "${{ github.workspace }}"
          
      - name: 添加SUSFS 配置项
        if: inputs.susfs_enable
        run: |
          cd kernel_workspace
          DEFCONFIG="./common/arch/arm64/configs/gki_defconfig"
          for opt in \
            "CONFIG_KSU_SUSFS=y" \
            "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" \
            "CONFIG_KSU_SUSFS_SUS_PATH=y" \
            "CONFIG_KSU_SUSFS_SUS_MOUNT=y" \
            "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y" \
            "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y" \
            "CONFIG_KSU_SUSFS_SUS_KSTAT=y" \
            "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" \
            "CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y" \
            "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" \
            "CONFIG_KSU_SUSFS_ENABLE_LOG=y" \
            "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" \
            "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" \
            "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" \
            "CONFIG_KSU_SUSFS_SUS_MAP=y"; do
            if ! grep -q "^$opt" "$DEFCONFIG"; then
              echo "$opt" >> "$DEFCONFIG"
            fi
          done
          
      - name: 添加 KSU & 其他配置项
        run: |
          cd kernel_workspace
          DEFCONFIG="./common/arch/arm64/configs/gki_defconfig"
          
          if [[ ${{ github.event.inputs.ksu_type }} != "none" ]]; then
            if ! grep -q "^CONFIG_KSU=y" "$DEFCONFIG"; then
              echo "CONFIG_KSU=y" >> "$DEFCONFIG"
            fi
          fi
          
          if [[ ${{ github.event.inputs.kpm_enable }} == 'builtin' && ( "${{ github.event.inputs.ksu_type }}" == "sukisu" || "${{ github.event.inputs.ksu_type }}" == "resukisu" ) ]]; then
            if ! grep -q "^CONFIG_KPM=y" "$DEFCONFIG"; then
              echo "CONFIG_KPM=y" >> "$DEFCONFIG"
            fi
          fi
          
          # Mountify 支持
          if ! grep -q "^CONFIG_TMPFS_XATTR=y" "$DEFCONFIG"; then
            echo "CONFIG_TMPFS_XATTR=y" >> "$DEFCONFIG"
          fi
          if ! grep -q "^CONFIG_TMPFS_POSIX_ACL=y" "$DEFCONFIG"; then
            echo "CONFIG_TMPFS_POSIX_ACL=y" >> "$DEFCONFIG"
          fi
          
          # LZ4KD 支持
          if [[ "${{ github.event.inputs.lz4kd_enable }}" == "true" ]]; then
            for opt in "CONFIG_ZSMALLOC=y" "CONFIG_CRYPTO_LZ4HC=y" "CONFIG_CRYPTO_LZ4K=y" "CONFIG_CRYPTO_LZ4KD=y" ; do
              if ! grep -q "^$opt" "$DEFCONFIG"; then
                echo "$opt" >> "$DEFCONFIG"
              fi
            done
          fi
          
          # O2 优化
          if ! grep -q "^CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y" "$DEFCONFIG"; then
            echo "CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y" >> "$DEFCONFIG"
          fi
          
          # 禁用 defconfig 检查
          sed -i 's/check_defconfig//' ./common/build.config.gki 2>/dev/null || true
          
          # 跳过 headers 安装
          if ! grep -q "^CONFIG_HEADERS_INSTALL=n" "$DEFCONFIG"; then
            echo "CONFIG_HEADERS_INSTALL=n" >> "$DEFCONFIG"
          fi
          
      - name: 启用网络功能增强优化配置
        if: inputs.better_net
        run: |
          cd "${{ github.workspace }}/kernel_workspace"
          DEFCONFIG="./common/arch/arm64/configs/gki_defconfig"
          cat >> "$DEFCONFIG" << 'EOF'
          CONFIG_BPF_STREAM_PARSER=y
          CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
          CONFIG_NETFILTER_XT_SET=y
          CONFIG_IP_SET=y
          CONFIG_IP_SET_MAX=65534
          CONFIG_IP_SET_BITMAP_IP=y
          CONFIG_IP_SET_BITMAP_IPMAC=y
          CONFIG_IP_SET_BITMAP_PORT=y
          CONFIG_IP_SET_HASH_IP=y
          CONFIG_IP_SET_HASH_IPMARK=y
          CONFIG_IP_SET_HASH_IPPORT=y
          CONFIG_IP_SET_HASH_IPPORTIP=y
          CONFIG_IP_SET_HASH_IPPORTNET=y
          CONFIG_IP_SET_HASH_IPMAC=y
          CONFIG_IP_SET_HASH_MAC=y
          CONFIG_IP_SET_HASH_NETPORTNET=y
          CONFIG_IP_SET_HASH_NET=y
          CONFIG_IP_SET_HASH_NETNET=y
          CONFIG_IP_SET_HASH_NETPORT=y
          CONFIG_IP_SET_HASH_NETIFACE=y
          CONFIG_IP_SET_LIST_SET=y
          CONFIG_IP6_NF_NAT=y
          CONFIG_IP6_NF_TARGET_MASQUERADE=y
          EOF
          cd common
          if [ ! -d "../oppo_oplus_realme_sm8750" ]; then
            echo "正在克隆补丁仓库…"
            git clone --depth=1 https://github.com/ukp6/oppo_oplus_realme_sm8750.git ../oppo_oplus_realme_sm8750
          fi
          if [ -f "../oppo_oplus_realme_sm8750/other_patch/config.patch" ]; then
            if patch -p1 -N --fuzz=3 < "../oppo_oplus_realme_sm8750/other_patch/config.patch"; then
              echo "✅ config.patch 应用成功"
            else
              echo "⚠️ config.patch 应用失败，继续构建"
            fi
          else
            echo "⚠️ config.patch 文件不存在，跳过"
          fi

      - name: 添加 BBR 等一系列拥塞控制算法
        run: |
          if [[ "${{ github.event.inputs.bbr_enable }}" != "false" ]]; then
            echo "正在添加BBR等拥塞控制算法..."
            cd kernel_workspace
            DEFCONFIG="./common/arch/arm64/configs/gki_defconfig"
            
            for opt in \
              "CONFIG_TCP_CONG_ADVANCED=y" \
              "CONFIG_TCP_CONG_BBR=y" \
              "CONFIG_TCP_CONG_CUBIC=y" \
              "CONFIG_TCP_CONG_VEGAS=y" \
              "CONFIG_TCP_CONG_NV=y" \
              "CONFIG_TCP_CONG_WESTWOOD=y" \
              "CONFIG_TCP_CONG_HTCP=y" \
              "CONFIG_TCP_CONG_BRUTAL=y"; do
              if ! grep -q "^$opt" "$DEFCONFIG"; then
                echo "$opt" >> "$DEFCONFIG"
              fi
            done
            
            if [[ "${{ github.event.inputs.bbr_enable }}" == "default" ]]; then
              if ! grep -q "^CONFIG_DEFAULT_TCP_CONG=bbr" "$DEFCONFIG"; then
                echo "CONFIG_DEFAULT_TCP_CONG=bbr" >> "$DEFCONFIG"
              fi
            else
              if ! grep -q "^CONFIG_DEFAULT_TCP_CONG=cubic" "$DEFCONFIG"; then
                echo "CONFIG_DEFAULT_TCP_CONG=cubic" >> "$DEFCONFIG"
              fi
            fi
          fi

      - name: 启用ADIOS IO调度器
        if: inputs.adios_enable
        run: |
          echo "正在启用ADIOS IO调度器…"
          cd kernel_workspace
          DEFCONFIG="./common/arch/arm64/configs/gki_defconfig"
          if ! grep -q "^CONFIG_MQ_IOSCHED_ADIOS=y" "$DEFCONFIG"; then
            echo "CONFIG_MQ_IOSCHED_ADIOS=y" >> "$DEFCONFIG"
          fi
          if ! grep -q "^CONFIG_MQ_IOSCHED_DEFAULT_ADIOS=y" "$DEFCONFIG"; then
            echo "CONFIG_MQ_IOSCHED_DEFAULT_ADIOS=y" >> "$DEFCONFIG"
          fi

      - name: 启用Re-Kernel支持
        if: inputs.rekernel_enable
        run: |
          echo "正在启用Re-Kernel支持…"
          cd kernel_workspace
          DEFCONFIG="./common/arch/arm64/configs/gki_defconfig"
          if ! grep -q "^CONFIG_REKERNEL=y" "$DEFCONFIG"; then
            echo "CONFIG_REKERNEL=y" >> "$DEFCONFIG"
          fi

      - name: 强制关闭基带保护（如果用户未启用且源码默认开启）
        if: github.event.inputs.baseband_guard != 'true'
        run: |
          DEFCONFIG="./kernel_workspace/common/arch/arm64/configs/gki_defconfig"
          if grep -q "^CONFIG_BBG=y" "$DEFCONFIG"; then
            sed -i '/^CONFIG_BBG=y/d' "$DEFCONFIG"
            echo "✅ 已从 defconfig 中移除默认的 CONFIG_BBG=y"
          else
            echo "defconfig 中未发现 CONFIG_BBG=y，无需处理"
          fi
      
      - name: 启用内核级基带保护
        if: inputs.baseband_guard
        run: |
          set -e
          echo "正在启用内核级基带保护支持…"
          cd "${{ github.workspace }}/kernel_workspace"
          DEFCONFIG="./common/arch/arm64/configs/gki_defconfig"
          if ! grep -q "^CONFIG_BBG=y" "$DEFCONFIG"; then
            echo "CONFIG_BBG=y" >> "$DEFCONFIG"
          fi
          cd ./common
          curl -sSL https://github.com/cctv18/Baseband-guard/raw/master/setup.sh | bash
          echo "✅ 基带保护基础配置完成，LSM 将在编译前修正"

      - name: 添加制作名称
        run: |
          cd kernel_workspace
          echo "设置内核版本后缀..."
          if [[ -n "${{ github.event.inputs.kernel_suffix }}" ]]; then
            SUFFIX="${{ github.event.inputs.kernel_suffix }}"
          else
            SUFFIX="${{ env.KERNEL_NAME }}"
          fi
          DEFCONFIG="./common/arch/arm64/configs/gki_defconfig"
          # 使用内核官方方法设置 LOCALVERSION
          echo "CONFIG_LOCALVERSION=\"-${SUFFIX}\"" >> "$DEFCONFIG"
          echo "CONFIG_LOCALVERSION_AUTO=n" >> "$DEFCONFIG"
          # 清理 setlocalversion 中的干扰
          sed -i 's/${scm_version}//' ./common/scripts/setlocalversion 2>/dev/null || true

      - name: 预清理磁盘空间（释放资源）
        run: |
          echo "清理不必要的系统目录以释放磁盘空间..."
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          df -h
       
      - name: 构建内核
        run: |
          WORKDIR="$(pwd)"
          export PATH="/usr/lib/ccache:$PATH"
          export PATH="$WORKDIR/kernel_workspace/clang18/bin:$PATH"
          export PATH="$WORKDIR/kernel_workspace/build-tools/bin:$PATH"
          export CCACHE_LOGFILE="${{ github.workspace }}/kernel_workspace/ccache.log"
          export CCACHE_COMPILERCHECK="none"
          export CCACHE_BASEDIR="${{ github.workspace }}"
          export CCACHE_NOHASHDIR="true"
          export CCACHE_HARDLINK="true"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CCACHE_MAXSIZE="3G"

          cd kernel_workspace/common

          # ---------- 配置内核 ----------
          make -j$(nproc --all) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- \
            CC="ccache clang" LD="ld.lld" HOSTLD=ld.lld O=out \
            KCFLAGS+=-O2 KCFLAGS+=-Wno-error gki_defconfig

          if [[ "true" == "true" ]]; then
            echo "正在修正 .config 中的 LSM 链..."
            cd out
            if grep -q "^CONFIG_LSM=" .config; then
              if ! grep -q "baseband_guard" .config; then
                # 在行尾双引号前插入 ,baseband_guard，保留原有所有模块
                awk '/^CONFIG_LSM=/ { if (!/baseband_guard/) { sub(/"$/, ",baseband_guard\""); } }1' .config > .config.tmp && mv .config.tmp .config
                echo "✅ 已追加 baseband_guard 到 LSM 列表"
              else
                echo "baseband_guard 已存在，无需追加"
              fi
            else
              echo 'CONFIG_LSM="landlock,lockdown,yama,loadpin,safesetid,selinux,bpf,baseband_guard"' >> .config
              echo "✅ 已设置默认 LSM 列表（含 baseband_guard）"
            fi
            cd ..
          fi

          # ---------- 编译内核 ----------
          make -j$(nproc --all) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- \
            CC="ccache clang" LD="ld.lld" HOSTLD=ld.lld O=out \
            KCFLAGS+=-O2 KCFLAGS+=-Wno-error Image
            
      - name: 应用KPM并修补内核
        run: |
          if [[ ${{ github.event.inputs.kpm_enable }} == 'builtin' && ( "${{ github.event.inputs.ksu_type }}" == "sukisu" || "${{ github.event.inputs.ksu_type }}" == "resukisu" ) ]]; then
            echo "正在应用KPM并修补内核..."
            cd kernel_workspace/common/out/arch/arm64/boot
            curl -LO https://github.com/SukiSU-Ultra/SukiSU_KernelPatch_patch/releases/latest/download/patch_linux
            chmod +x patch_linux
            ./patch_linux
            rm -f Image
            mv oImage Image
          fi
          
      - name: 克隆 AnyKernel3 并打包
        run: |
          cd kernel_workspace
          git clone https://github.com/cctv18/AnyKernel3 --depth=1
          rm -rf ./AnyKernel3/.git
          cd AnyKernel3
          cp ../common/out/arch/arm64/boot/Image ./Image
          if [[ ! -f ./Image ]]; then
            echo "未找到内核镜像文件，构建可能出错"
            exit 1
          fi
          
          # 确定 KSU 类型名称
          case "${{ github.event.inputs.ksu_type }}" in
            sukisu) KSU_TYPENAME="SukiSU" ;;
            resukisu) KSU_TYPENAME="ReSukiSU" ;;
            ksunext) KSU_TYPENAME="KSUNext" ;;
            mksu) KSU_TYPENAME="MKSU" ;;
            ksu) KSU_TYPENAME="KSU" ;;
            *) KSU_TYPENAME="none" ;;
          esac
          
          # 打包
          if [[ -n "${{ github.event.inputs.kernel_suffix }}" ]]; then
            zip -r ../AnyKernel3_${KSU_TYPENAME}_${{ env.KSUVER }}_${{ env.KERNEL_VERSION }}_A15_${{ github.event.inputs.kernel_suffix }}.zip ./*
          else
            zip -r ../AnyKernel3_${KSU_TYPENAME}_${{ env.KSUVER }}_${{ env.KERNEL_VERSION }}_A15_${{ env.KERNEL_NAME }}.zip ./*
          fi

      - name: 上传 Ccache 调试日志
        if: always() && inputs.ccache_debug
        uses: actions/upload-artifact@v4
        with:
          name: ccache-debug-log
          path: ${{ github.workspace }}/kernel_workspace/ccache.log

      - name: 上传 ZIP 工件
        uses: actions/upload-artifact@v4
        with:
          name: Kernel_ZIP_Artifacts
          path: ${{ github.workspace }}/kernel_workspace/AnyKernel*.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      actions: read
      
    steps:
      - name: 下载 ZIP 工件
        uses: actions/download-artifact@v4
        with:
          name: Kernel_ZIP_Artifacts
          path: ./release_zips

      - name: 设置环境变量
        run: |
          if [[ -n "${{ github.event.inputs.kernel_suffix }}" ]]; then
            FULL_VERSION="${{ env.KERNEL_VERSION }}.89-${{ github.event.inputs.kernel_suffix }}"
          else
            FULL_VERSION="${{ env.KERNEL_VERSION }}.89-${{ env.KERNEL_NAME }}"
          fi
          echo "FULL_VERSION=$FULL_VERSION" >> $GITHUB_ENV
          
          TIME="$(TZ='Asia/Shanghai' date +'%y%m%d%H%M%S')"
          TIME_FORM="$(TZ='Asia/Shanghai' date +'%Y-%m-%d %H:%M:%S')"
          echo "TIME=$TIME" >> $GITHUB_ENV
          echo "TIME_FORM=$TIME_FORM" >> $GITHUB_ENV
          
          TAG_HEAD="OPPO+OPlus+Realme-A15-build"
          echo "TAG_HEAD=$TAG_HEAD" >> $GITHUB_ENV
          
          case "${{ github.event.inputs.ksu_type }}" in
            sukisu) KSU_TYPENAME="SukiSU Ultra" ;;
            resukisu) KSU_TYPENAME="ReSukiSU" ;;
            ksunext) KSU_TYPENAME="KernelSU Next" ;;
            mksu) KSU_TYPENAME="MKSU" ;;
            ksu) KSU_TYPENAME="KSU" ;;
            *) KSU_TYPENAME="无内置KSU" ;;
          esac
          echo "KSU_TYPENAME=$KSU_TYPENAME" >> $GITHUB_ENV
         
      - name: 创建发布
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "${{ env.TAG_HEAD }}-${{ env.TIME }}"
          name: "${{ env.TAG_HEAD }}-${{ env.FULL_VERSION }}"
          body: |
            ### 📱 欧加真 ${{ env.KSU_TYPENAME }} SM8750 通用内核 | 构建信息
            - 内核版本号: ${{ env.FULL_VERSION }}
            - 编译时间: ${{ env.TIME_FORM }}
            - 机型：欧加真骁龙8Elite通用 6.6.89 内核（基于一加13国际版 6.6.89 版官方OKI源码）
            - 特性：${{ env.KSU_TYPENAME }} + 风驰内核
            - susfs支持：${{ github.event.inputs.susfs_enable }}
            - KPM支持 ：${{ github.event.inputs.kpm_enable }}
            - LZ4支持：${{ github.event.inputs.lz4_enable }}
            - LZ4KD支持：${{ github.event.inputs.lz4kd_enable }}
            - 网络功能增强：${{ github.event.inputs.better_net }}
            - BBR/Brutal 等拥塞控制算法支持：${{ github.event.inputs.bbr_enable }}
            - ADIOS IO调度器支持：${{ github.event.inputs.adios_enable }}
            - Re-Kernel支持：${{ github.event.inputs.rekernel_enable }}
            - 内核级基带保护支持：${{ github.event.inputs.baseband_guard }}
            - SukiSU Ultra管理器下载：[SukiSU-Ultra](https://github.com/SukiSU-Ultra/SukiSU-Ultra/releases)
            - KernelSU Next管理器下载：[KernelSU-Next](https://github.com/KernelSU-Next/KernelSU-Next/releases)
            - MKSU管理器下载：[Magic-KernelSU](https://github.com/5ec1cff/KernelSU/actions)
            - KSU原版管理器下载：[KernelSU](https://github.com/tiann/KernelSU/releases)
            ### ⏫️ 更新内容：
            - 更新${{ env.KSU_TYPENAME }}至最新版本（${{ needs.build.outputs.ksuver }}）
            - (预留)
            ### 📋 安装方法 | Installation Guide
            1. 若你的手机已经安装了第三方Recovery（如TWRP)，可下载对应机型的AnyKernel刷机包后进入Recovery模式，通过Recovery刷入刷机包后重启设备；
            2. 若你的手机之前已有 root 权限，可在手机上安装[HorizonKernelFlasher](https://github.com/libxzr/HorizonKernelFlasher/releases)，在HorizonKernelFlasher中刷入AnyKernel刷机包并重启；
            3. 若你之前已刷入SukiSU Ultra内核，且SukiSU Ultra管理器已更新至最新版本，可在SukiSU Ultra管理器中直接刷入AnyKernel刷机包并重启；
            4. 刷入无lz4kd补丁版的内核前若刷入过lz4kd补丁版的内核，为避免出错，请先关闭zram模块；
            5. 由于KernelSU上游更新了元模块功能，最新版KSU管理器（包括除KernelSU Next以外的各分支）需要配合元模块(metamodule)才能正常挂载模块。目前的元模块包括[meta overlayfs](https://github.com/KernelSU-Modules-Repo/meta-overlayfs), [mountify](https://github.com/backslashxx/mountify), [meta magicmount](https://github.com/7a72/meta-magic_mount/), [meta magicmount rs](https://github.com/Tools-cx-app/meta-magic_mount/), [hybrid mount](https://github.com/YuzakiKokuban/meta-hybrid_mount)等。若你是第一次使用KSU或刚从旧版KSU管理器升级至新版，请先安装一个元模块，这样其他涉及系统挂载的模块才能正常运行；
            6. KernelPatch Next（即KPN）是一个独立于KSU的KPM实现，可以运行在任意KSU/面具环境中（不适用于Apatch），且不能与(Re)SukiSU内置的kpm功能共同使用，使用前请保证你的内核没有内置的kpm实现/修补。由于KPN的实现需要设置superkey，出于安全考虑，工作流脚本没有用统一的superkey自动修补内核，刷入后首次运行该模块时请对内核进行手动修补。
            #### ※※※刷写内核有风险，为防止出现意外导致手机变砖，在刷入内核前请务必用[KernelFlasher](https://github.com/capntrips/KernelFlasher)等软件备份boot等关键启动分区!※※※
          draft: false
          prerelease: false
          files: |
            release_zips/AnyKernel3_*.zip
